<p id="notice"><%= notice %></p>
<h1 class="part_out">John, Cindy & Family</h1>


<%= link_to 'Add New Photo', new_albumsix_path, class:"btn btn-primary add" %>

<div class="somespace"></div>


  <div>
    <% @albumsixs.each do |albumsix| %>
      <div class="col-md-6">
        
        <% if albumsix.avatar.file? %>        
        <a> <%= image_tag albumsix.avatar, class:"img-responsive photoalbums"%></a>
        <div class="phototitle"><%= albumsix.title %><% if current_user.id == albumsix.user.id %>
        <div class="dropdown2">
  <button class="dropbtn2">Options</button>
  <div class="dropdown-content2">
    <a> <%= link_to 'Edit', edit_albumsix_path(albumsix)%> </a>
    <a>  <%= link_to 'Delete', albumsix, method: :delete, data: { confirm: 'Are you sure?' } %></a>
  </div>
</div>
       <%end%>
       <p class="like_circle2"><%= link_to "", like_albumsix_path(albumsix), class:"glyphicon glyphicon-thumbs-up like2"%><a class="count_score2"><%= albumsix.score %></a></div>
<%end%>
        
      </div>
      <div class="somespace"></div>
    <% end %>
<div class="somemorespace"></div>